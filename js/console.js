// Add logging to debug web console I/O
console.debug('Initializing web console...');

function sendInput(input) {
    console.debug('Sending input:', input);
    // Existing code
}

function receiveOutput(output) {
    console.debug('Received output:', output);
    // Existing code
}

function compileAndRun(code, language) {
    console.debug(`Compiling ${language} code:`, code);
    // Existing code
}
